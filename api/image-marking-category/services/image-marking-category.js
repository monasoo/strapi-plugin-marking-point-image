'use strict';

/**
 * image-marking-category service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::image-marking-category.image-marking-category');
