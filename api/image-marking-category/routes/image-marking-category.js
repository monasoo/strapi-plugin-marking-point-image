'use strict';

/**
 * image-marking-category router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::image-marking-category.image-marking-category');
