'use strict';

/**
 * mark-pin-relation service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::mark-pin-relation.mark-pin-relation');
