'use strict';

/**
 * image-marking-image service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::image-marking-image.image-marking-image');
