'use strict';

/**
 * image-marking-image router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::image-marking-image.image-marking-image');
