'use strict';

/**
 * image-marking-product router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::image-marking-product.image-marking-product');
