'use strict';

/**
 * image-marking-product service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::image-marking-product.image-marking-product');
