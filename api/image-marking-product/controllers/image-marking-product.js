'use strict';

/**
 * image-marking-product controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::image-marking-product.image-marking-product');
